@import "~normalize.css"
@import "./variables"

*
*::before
*::after
  box-sizing border-box

*
  padding 0
  margin 0

html
  font-family PingFang SC,Helvetica,Arial,sans-serif
  font-size 12px

img
  max-width 100%
